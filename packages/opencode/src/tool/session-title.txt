Use this tool to retrieve the title of the current session.

This tool returns the current session's title as a string. The title is set when the session is created and can be updated using the set_current_session_title tool.
